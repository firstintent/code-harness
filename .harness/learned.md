# Learned

Project knowledge accumulated across sessions. Extracted from auto memory
and direct experience. All sessions (including after account switches) read this file.

This file is imported by CLAUDE.md via @.harness/learned.md so it loads at session start.

## Build & Test

<!-- Add project-specific build/test commands as you discover them. Example:
- Run tests: `pnpm test`
- Database migration: `pnpm db:migrate` (must run before tests)
- Dev server: `pnpm dev` on port 3000
-->

## Known Pitfalls

<!-- Add gotchas discovered during development. Example:
- src/utils/date.ts has a timezone bug, use dayjs instead
- PostgreSQL connection pool max is 20, silently fails above that
-->

## User Preferences

<!-- Add preferences discovered from user interactions. Example:
- Prefers functional style, avoid classes
- Error messages should be user-readable, no stack traces
- Commit messages in English, conventional commits format
-->
